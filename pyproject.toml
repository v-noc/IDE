[project]
name = "v-noc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["pydantic[email]>=2.11.7"]

[tool.uv]
dev-dependencies = [
    "pytest>=8.4.1",
    "pytest-asyncio>=1.1.0",
]

[tool.uv.workspace]
members = ["src/backend", "src/vn_logger", "src/lsp/py"]

[project]
name = "v-noc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic[email]>=2.11.7",
]

[tool.uv.workspace]
members = ["src/backend"]

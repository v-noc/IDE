/**
 * Typewriter timing is driven by {@link WalkthroughEngine} (store updates each character).
 * This hook is reserved for future client-side effects (e.g. reduced motion) without
 * duplicating the engine loop.
 */
export function useTypewriter(): void {}

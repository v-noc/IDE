### Common Recipes

- Idempotent upsert of `links_to` edge
- Paginated traversal of `contains` graph
- Resolving imports to packages and creating `uses_import` edges

Tips
- Always use bind vars in AQL
- Keep transactions short and bounded 
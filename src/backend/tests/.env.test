APP_ENV=test
# Walkthrough LLM — always fake in unit tests
WALKTHROUGH_LLM=fake
# TerminusDB (used by tests)
TERMINUS_HOST=http://localhost:6363
TERMINUS_DB=test_db
TERMINUS_USER=admin
TERMINUS_KEY=root
TERMINUS_TEAM=admin
# Other
PORT=8001
GEMINI_API_KEY=test_key
